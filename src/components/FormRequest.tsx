import { beba } from "@/constants/fonts";
import ContactForm from "./Form";

const FormRequest = () => {
  return (
    <section className="bg-nightest w-full rounded-xl py-8 px-4 lg:p-0 md:p-8">
      <ContactForm />
    </section>
  );
};

export default FormRequest;
